spring:
  application:
    name: eureka-service
  cloud:
    config:
      uri: http://config-service:8081
      fail-fast: true
      retry:
        initial-interval: 2000
        max-interval: 5000
        multiplier: 1.5
        max-attempts: 20    # 20 intentos