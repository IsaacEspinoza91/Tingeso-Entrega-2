server:
  port: 8761
eureka:
  instance:
    hostname: eureka-service  # Coincide con el nombre del servicio en docker-compose.yml
  client:
    fetch-registry: false
    register-with-eureka: false
    service-url: 
      default-zone: http://${eureka.instance.hostname}:${server.port}/eureka/