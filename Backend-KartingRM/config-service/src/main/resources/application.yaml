server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/IsaacEspinoza91/Tingeso-Entrega-2
          clone-on-start: true
          default-label: main
          search-paths: config-data
  application:
    name: config-service