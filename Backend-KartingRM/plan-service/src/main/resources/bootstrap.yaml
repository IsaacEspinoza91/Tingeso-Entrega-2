spring:
  application:
    name: plan-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
  datasource:
    url: jdbc:postgresql://localhost:5432/plan_db
    username: postgres
    password: 123
  jpa:
    hibernate:
      ddl-auto: update